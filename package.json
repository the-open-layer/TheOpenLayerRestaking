{
    "name": "restaking",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build-fe": "yarn workspace @the-open-layer/fe run build",
        "build-contract": "yarn workspace @the-open-layer/contract run build ReStaking",
        "test": "yarn workspace @the-open-layer/contract test",
        "run": "yarn workspace @the-open-layer/contract blueprint run"
    },
    "devDependencies": {
        "@types/jest": "^29.5.14",
        "@types/node": "^22.8.7",
        "jest": "^29.7.0",
        "prettier": "^3.3.3",
        "@ton/ton": "^15.1.0",
        "@ton/core": "^0.59.0",
        "@ton/crypto": "^3.3.0",
        "ts-jest": "^29.2.5",
        "ts-node": "^10.9.2",
        "typescript": "^5.6.3"
    },
    "workspaces": [
        "packages/*"
    ]
}
